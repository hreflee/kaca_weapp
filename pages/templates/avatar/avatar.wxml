<!--avatar.wxml-->
<template name="avatar">
  <view bindtap="changeName" class="avatar">
    <image class="avatar-img" mode="widthFix" src="{{img}}"></image>
    <view class="avatar-name">{{name}}</view>
  </view>
</template>